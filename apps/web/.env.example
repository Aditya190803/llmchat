JINA_API_KEY=
# Optional: pin the transcription model for voice input.
# Defaults to the gateway's Whisper model (turbo preferred).
AI_GATEWAY_TRANSCRIBE_MODEL=

# Web search + page reading (free tier, one key from agent.tinyfish.ai).
# Primary provider; falls back to SERPER_API_KEY, then DuckDuckGo.
TINYFISH_API_KEY=
